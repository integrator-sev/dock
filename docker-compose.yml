version: '3.7'
services:
  maven:
    build: mvn
    volumes:
      - app:/webapps

  tomcat:
    build: tom
    image: mytomcat-experimental
    ports:
      - "8888:8080"
    volumes:
      - app:/opt/for_tom
volumes:
  app:

